# Коллекция `services` (§7, /relocation/services/). Пусто — наполняется реальными
# сервисами по входящему спросу. Файлы пар ru/uk: services/{ru,uk}/{slug}.md.
# .gitkeep не матчится glob-паттерном '**/*.{md,mdx}' → коллекция остаётся пустой.
