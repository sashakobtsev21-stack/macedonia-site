# Технический аудит — North Macedonia Guidebook · 2026-06-22

**Стадия:** скелет. 2 demo-сид-материала (статья `lake-ohrid` category=attractions + маршрут `north-macedonia-7-day-itinerary`), оба `demo:true`/noindex, тройки en/ru/uk. **0 городов, 0 ресторанов, 0 услуг, 0 новостей.** Движок Astro 6.4.6 SSG.

Легенда находок: **[BUG]** сломано · **[ГРУЗ-ХВОСТ]** наследие Грузии · **[НЕТ-КОНТЕНТА]** красно только из-за отсутствия контента. Статус: ✅ закрыто в этом прогоне · ⏳ открыто (→ `ROADMAP-FIX.md`).

## Сводка
| Проверка | Результат |
|---|---|
| `astro check` | ✅ 0 ошибок · 0 предупреждений · 3 hints |
| Сборка | ✅ 61 страница, без ошибок |
| `npm run qa` гейты | ✅ check · build · test · links · lint — все зелёные |
| Lighthouse mobile (`qa:browser`) | ✅ a11y / SEO / best-practices = **100** на всех проверенных; perf 86–92 |
| Адаптив (`qa-responsive`) | ✅ 0 горизонтальных переполнений (10 шаблонов × 5 ширин 320–1280) |
| `npm audit` | prod — 2 low (esbuild dev-server, не в прод-бандле); dev — 24 (dev-tooling). **high/critical — НЕТ** |
| Секреты в репо | не найдено (партнёрские ID Trip.com/Travelpayouts публичны по дизайну) |

## Безопасность
- ✅ **CSP** (`public/_headers`): строгий `default-src 'self'`; GA4 inline через sha256 (совпадает с собранным скриптом); `frame-ancestors 'none'`, `object-src 'none'`, без `unsafe-eval`.
- ✅ **/go/ редиректор** (`worker/index.ts`): НЕ open-redirect — резолвит только ключи из `partners.json`, целевой URL обязан быть `https:`, иначе безопасный фолбэк на свой сайт. Языковая сегментация `byLang` (MON-R1-3) — обратно совместима.
- ✅ **robots**: `Disallow: /go/`; `sitemap-index.xml` по трём языкам, demo/services исключены.

## Находки

### [ГРУЗ-ХВОСТ] — ✅ закрыто
- ✅ `scripts/qa.mjs:21` — `ORIGIN = 'https://georgiaguidebook.com'` (домен Грузии) давал **180 ложных critical** «hreflang: целевой страницы нет» (относительные ключи pageMap не совпадали с абсолютными hreflang-таргетами после установки реального `site`). → `macedoniaguidebook.com`. **Это и был корень NO-GO.**
- ✅ `src/layouts/InsuranceHub.astro:49` — мост на русско-слаговую `/insurance/kak-vybrat-strahovku/` → англ. `how-to-choose-travel-insurance` + условный рендер.
- ✅ `scripts/qa-lighthouse.mjs` / `qa-responsive.mjs` — тестировали грузинские URL (`/goroda/tbilisi/` …) → ложный NO-GO; переведены на македонские (закрыто ранее, перепроверено).
- ✅ `public/og-default.jpg` — был храм Гергети + Казбек (Грузия), дефолт шеринга → озеро Охрид (закрыто ранее).
- _Закрыто в предыдущих прогонах:_ favicon/иконки приложения (флаг СМ), `partners.json` kwd, hero-фото, орфан `strahovka/g1,g2`, `addressCountry GE→MK`, грузинские города в `EdaDirectory`, устаревшие комментарии.

### [BUG] — ✅ закрыто
- ✅ Битые внутренние ссылки (`test:links`, 24 шт.): мосты на ещё не созданные pillar `/food/what-to-eat-in-north-macedonia/`, city-гайды `/cities/{skopje,ohrid,bitola}/`, insurance-гайд → **условный рендер** (ссылка появляется автоматически, когда статья создана). Файлы: `CityFoodPage.astro`, `EdaDirectory.astro`, `InsuranceHub.astro`.
- ✅ demo-маршрут индексировался (нет `noindex`) → добавлен (ранее).
- ✅ title главной >60 → 54/54/53 en/ru/uk (ранее).
- ✅ контраст подписей погоды 4.52:1 → 88% white (ранее).

### [НЕТ-КОНТЕНТА] — ожидаемо для скелета
- Пустые коллекции cities/restaurants/services/news — по дизайну (§11).
- 13 хабов × 3 языка рендерятся пустыми (EmptyHint) — норма.
- Мосты на pillar/города/insurance-гайд теперь скрыты до появления статей.

### ⏳ Открыто (→ ROADMAP-FIX.md)
- **[perf]** hero `ohrid-kaneo.webp` >200КБ (LCP главной, на 2 страницах) — единственная оставшаяся «критическая» в dist-аудите `qa.mjs`. Оптимизировать в фото-фазе.
- **[a11y, low]** `qa.mjs` помечает «пустой alt» у декоративного `mk-flag` и `hero__bg` — это намеренный `alt=""`/`aria-hidden` для декоративных (false-positive). При желании — закрепить явными ARIA.
- Полный добор фото у обоих сид-материалов до нормы (отложено с Этапа 1).

## UI/UX (десктоп + мобайл)
- Хедер, выпадающее меню, переключатель языка, карта Leaflet (click-to-load), погодная полоса — рабочие (проверено headless-скриншотами). Адаптив без горизонтального скролла на 320–1280.
- Демо-страницы: бейдж «Пример» + `noindex`; обложки теперь реальные (Охрид / Матка, webp ≤200КБ).

## Вердикт
Технически репо к публикации готов: все гейты зелёные, безопасность без high/critical, a11y/SEO/BP=100. Открыто — одна перф-оптимизация hero (не блокер публикации) и owner-действия по деплою/домену/GSC.
